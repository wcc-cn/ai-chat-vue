# Ollama Configuration
# Default Ollama URL: http://localhost:11434
# The Vite dev server proxy will forward /api requests to this URL

# Optional: Override Ollama host (default: localhost)
# OLLAMA_HOST=localhost

# Optional: Override Ollama port (default: 11434)
# OLLAMA_PORT=11434
